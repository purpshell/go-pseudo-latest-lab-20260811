# Go pseudo-version latest lab

Disposable public module for testing whether `@latest` selects an untagged
pseudo-version after every indexed semantic version is self-retracted and the
Git tags are removed.

This repository contains no production code.
